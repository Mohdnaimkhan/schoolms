# Copy this file to .env (or export these as real environment variables)
# before running the application. Never commit the real .env file.

DB_URL=jdbc:mysql://localhost:3306/school_db
DB_USERNAME=root
DB_PASSWORD=change-me
